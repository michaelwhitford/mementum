💡 S-expression parser validates grammar before execution. Tokenizer uses string-prefix matching for Unicode-correct emoji detection — handles surrogate pairs, variation selectors, and ZWJ sequences. Known symbols (7 core) tokenize as :emoji type; unknown emojis fall through to :symbol and get caught by validation. Constraints checked before git commands run.
