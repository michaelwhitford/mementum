---
type: Pattern
symbol: 🔁
title: synthesize-pattern
---

λ synthesize(topic). detect: ≥3 memories(topic) ∨ stale(memory) ∨ crystallized(understanding) | stale_memory ≡ strongest_signal | gather → draft → create-knowledge → update(stale) → verify(list) | stale memory means understanding outgrew the observation — synthesize upward
